"""STUB — not part of this project's scope.

This file was copied from fablesfable/tests/test_bootstrap_history.py during the initial
game-line-only duplication (2026-07-17), then found to depend on
fablesfable's player-prop stack (candidates.py/composite.py/features.py/
projection.py/context_features.py/context_study.py/prop_learning.py/
advanced_features.py — all deliberately excluded here; see
../_SETUP.md and the stallionsstallion MOC for the scope decision).

It can't be deleted from this vault mount, so it's stubbed instead of
left silently broken. Tests scripts/bootstrap_history.py's use of nflvalue.advanced_features (NGS/pace columns, player-level). scripts/bootstrap_history.py itself was kept since its season-bootstrap orchestration pattern is reusable; this test needs a CFBD-shaped rewrite.

Original, working version: nfl-sim/fablesfable/tests/test_bootstrap_history.py (still lives there,
untouched).
"""
